package app.cloudcoffee.securepassword.framework

enum class FailureCode {
    DOMAIN_ERROR,
    SOMETHING_MAPPING,
    GENERAL,
    PARSING_ERROR,
    NULL_VALUE,
    ENCRYPTION_ERROR,
    OPERATION_CANCELED,
    NETWORK_EXCEPTION
}